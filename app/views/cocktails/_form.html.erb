<p><%= simple_form_for(@cocktail) do |f| %>
    <%= f.input :name, label: "Cocktail name  " %>
    <%= f.input :photo %>
    <%= f.input :photo_cache, as: :hidden %>
    <%= f.button :submit, class: "btn-treehouse" %>
  <% end %>
</p>
